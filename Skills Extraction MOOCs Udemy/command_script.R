source("Script/SKILL_0_esco-dataset-reduction.R")

rm(list = ls())

source("Script/SKILL_1_preparation.R")

rm(list = ls())

source("Script/SKILL_2_text-cleaning-tier-one.R")

rm(list = ls())

source("Script/SKILL_3_text-cleaning-tier-two.R")

rm(list = ls())

source("Script/SKILL_4_semantic-similarity.R")

rm(list = ls())

source("Script/SKILL_5_sequential-search.R")

rm(list = ls())

source("Script/SKILL_6_single-word-skills.R")

rm(list = ls())

source("Script/SKILL_7_final-skill-column.R")
